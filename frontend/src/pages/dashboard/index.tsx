import { Layout } from "../../components/Layout/index";
const Dashboard: React.FC = () => {
  return (
    <Layout type={"dashboard"} title="Dashboard">
      <h1>Dashboard</h1>
    </Layout>
  );
};

export default Dashboard;
